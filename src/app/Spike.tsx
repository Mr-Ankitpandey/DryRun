/** Engine spike page (owned by WP-A). Renders a Run from a module or fixture
 *  through the scene builder and renderers with bare visuals. */
export default function Spike() {
  return (
    <main className="min-h-dvh bg-bg text-ink px-4 py-10">
      <h1 className="font-display text-2xl">Engine spike</h1>
      <p className="mt-2 text-ink-2">Renderers are not built yet.</p>
    </main>
  );
}
