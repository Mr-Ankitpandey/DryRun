/** Shared class strings for the learning screens, so the four screens stay one system. */

/** Inline text link: ink text, rule underline that darkens on hover (as on /styleguide). */
export const textLink = 'rounded-xs text-ink underline decoration-rule underline-offset-4 hover:decoration-ink';

/** Screen title (h1): display face, 31 px step of the scale. */
export const screenTitle = 'font-display text-3xl text-ink';

/** Section title (h2): display face, 20 px step. */
export const sectionTitle = 'font-display text-xl text-ink';

/** One-line explanation under a title. */
export const lede = 'mt-2 max-w-prose text-base text-ink-2';
