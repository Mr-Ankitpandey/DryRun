/** Shared layout constants in abstract scene units (the renderer maps them to
 *  the viewport through the SVG viewBox). */

export const PAD = 24;
/** Default abstract width of a scene. */
export const SCENE_WIDTH = 900;
